{
    "name": "tree-sitter-sexp",
    "version": "1.0.0",
    "description": "S-expression grammar for tree-sitter",
    "main": "bindings/node",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Frzoen",
    "license": "ISC",
    "dependencies": {
        "nan": "^2.18.0"
    },
    "devDependencies": {
        "tree-sitter-cli": "^0.21.0"
    }
}
